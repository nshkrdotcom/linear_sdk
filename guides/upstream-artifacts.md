# Upstream Artifacts

This repo keeps three upstream-oriented inputs:

- committed introspection snapshot
- curated GraphQL documents
- copied official reference manifests from the upstream Linear SDK

The copied manifests are reference-only.
They are kept to document how the upstream JS SDK organizes schema/doc/sdk/test
generation, not because this repo runs the upstream Node codegen toolchain.
